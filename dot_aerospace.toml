# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = ['exec-and-forget sketchybar']

# Notify Sketchybar about workspace updates
exec-on-workspace-change = ['/bin/bash', '-c', 'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE']
# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 1

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Keep cursor stationary when switching monitors (closer to yabai's defaults)
on-focused-monitor-changed = []

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Matches the padding/window_gap values from the yabai config
[gaps]
    inner.horizontal = 1
    inner.vertical =   1
    outer.left =       1
    outer.bottom =     1
    outer.top =        1
    outer.right =      1

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # Hyper-style launcher bindings
    cmd-alt-ctrl-o = '''exec-and-forget osascript -e '
    tell application "Terminal"
        do script "cd \"/Users/philipnickel/Library/Mobile Documents/iCloud~md~obsidian/Documents\""
        activate
    end tell'
    '''
    cmd-alt-ctrl-c = 'exec-and-forget open -na "Google Chrome" --args --new-window'
    # Toggle root layout type between tiles ↔ accordion
    cmd-alt-ctrl-enter= 'layout tiles accordion'

    # (Optional) explicit setters if you ever want direct jumps
    cmd-alt-ctrl-t = 'layout tiles'
    cmd-alt-ctrl-a = 'layout accordion'


    # Focus bindings (mirrors skhd right-command navigation)
    cmd-alt-ctrl-h = 'focus left'
    cmd-alt-ctrl-j = 'focus down'
    cmd-alt-ctrl-k = 'focus up'
    cmd-alt-ctrl-l = 'focus right'

    # Move/warp window bindings
    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'

    # Directional resize bindings (smart keeps orientation awareness)
    cmd-alt-ctrl-shift-h = 'resize smart -25'
    cmd-alt-ctrl-shift-l = 'resize smart +25'
    cmd-alt-ctrl-shift-k = 'resize smart-opposite -25'
    cmd-alt-ctrl-shift-j = 'resize smart-opposite +25'

    # Fullscreen toggle
#cmd-alt-ctrl-enter = 'fullscreen'

    # Workspace navigation (mirrors f18 + number)
    cmd-alt-ctrl-1 = 'workspace 1'
    cmd-alt-ctrl-2 = 'workspace 2'
    cmd-alt-ctrl-3 = 'workspace 3'
    cmd-alt-ctrl-4 = 'workspace 4'
    cmd-alt-ctrl-5 = 'workspace 5'
    cmd-alt-ctrl-6 = 'workspace 6'
    cmd-alt-ctrl-n = 'workspace-back-and-forth'


    # Move active window then follow focus (mirrors f18 + shift + number)
    cmd-alt-ctrl-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
    cmd-alt-ctrl-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
    cmd-alt-ctrl-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
    cmd-alt-ctrl-shift-4 = ['move-node-to-workspace 4', 'workspace 4']
    cmd-alt-ctrl-shift-5 = ['move-node-to-workspace 5', 'workspace 5']
    cmd-alt-ctrl-shift-6 = ['move-node-to-workspace 6', 'workspace 6']

    # Quick utility bindings
    cmd-alt-ctrl-y = 'reload-config'
    cmd-alt-ctrl-z = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    cmd-alt-ctrl-h = ['join-with left', 'mode main']
    cmd-alt-ctrl-j = ['join-with down', 'mode main']
    cmd-alt-ctrl-k = ['join-with up', 'mode main']
    cmd-alt-ctrl-l = ['join-with right', 'mode main']

    down = 'volume down'
    up = 'volume up'
    shift-down = ['volume set 0', 'mode main']
